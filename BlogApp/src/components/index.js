// Importing components from their respective paths
import Select from "./Select"; // Importing the Select component from the Select file
import Header from "./Header/Header"; // Importing the Header component from the Header directory
import Footer from "./Footer/Footer"; // Importing the Footer component from the Footer directory
import Container from "./container/Container"; // Importing the Container component from the container directory
import Logo from "./Logo"; // Importing the Logo component from the Logo file
import LogoutBtn from "./Header/LogoutBtn"; // Importing the LogoutBtn component from the Header directory
import RTE from "./RTE"; // Importing the RTE (Rich Text Editor) component
import Signup from "./Signup"; // Importing the Signup component
import Login from "./Login"; // Importing the Login component
import Button from "./Button"; // Importing the Button component
import PostForm from "./post-form/PostForm"; // Importing the PostForm component from the post-form directory
import PostCard from "./PostCard"; // Importing the PostCard component
import AuthLayout from "./AuthLayout"; // Importing the AuthLayout component
import Input from "./Input"; // Importing the Input component

// Exporting all imported components for easy import elsewhere in the project
export {
    Header, // Exporting the Header component
    Footer, // Exporting the Footer component
    Container, // Exporting the Container component
    Logo, // Exporting the Logo component
    LogoutBtn, // Exporting the LogoutBtn component
    RTE, // Exporting the RTE component
    Signup, // Exporting the Signup component
    Login, // Exporting the Login component
    Button, // Exporting the Button component
    PostForm, // Exporting the PostForm component
    PostCard, // Exporting the PostCard component
    AuthLayout, // Exporting the AuthLayout component
    Input, // Exporting the Input component
    Select, // Exporting the Select component
};
